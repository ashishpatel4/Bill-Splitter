<%- include("partials/header") %>

<div class="text-center">
    <h1 class="text-center"><%=bill.name%></h1>
    <h3 class=text-center>paid by:- <%=bill.payer%></h3>

    <%bill.people.forEach( (p) =>{%>
        <%if(p!=bill.payer){%>
        <h5><%=p%> has to pay <%= bill.total/bill.people.length %> to <%= bill.payer %></h5>
        <%}%>
    <%})%>
</div>
<div class="text-center">
    <a href="create.ejs" class="btn btn-warning">do again</a>
</div>
